#!/usr/bin/env bash
set -e
set -u
set -o pipefail

if [ $# -ne 2 ]; then
	echo "Usage: yoink <remote-directory> <user@host>" >&2
	echo "Downloads from remote to current directory" >&2
	exit 1
fi

remote_dir="$1"
ssh_server="$2"

exec scp -r "${ssh_server}:${remote_dir}" .
