# .zshrc.local.example
#
# This file shows examples of machine-specific configurations that should NOT
# be tracked in git. Copy this file to ~/.zshrc.local and customize for your machine.
#
# Usage:
#   cp ~/.dotfiles/zsh/.zshrc.local.example ~/.zshrc.local
#   nano ~/.zshrc.local  # Edit with your machine-specific settings
#

# ------------------------------------------------------------------------------
# Terminal Colors by Hostname
# ------------------------------------------------------------------------------
# Different visual indicators based on which machine you're on

# Option 1: iTerm2 badge (macOS only) - Shows hostname in corner
# if [[ "$TERM_PROGRAM" == "iTerm.app" ]]; then
#   echo -e "\033]1337;SetBadgeFormat=$(echo -n "$(hostname -s)" | base64)\a"
# fi

# Option 2: Set terminal tab color via escape codes (iTerm2, some other terminals)
# case "$(hostname -s)" in
#   production-server)
#     echo -ne "\033]6;1;bg;red;brightness;255\a"    # Red tab
#     echo -ne "\033]6;1;bg;green;brightness;0\a"
#     echo -ne "\033]6;1;bg;blue;brightness;0\a"
#     ;;
#   dookbox)
#     echo -ne "\033]6;1;bg;red;brightness;0\a"      # Blue tab
#     echo -ne "\033]6;1;bg;green;brightness;150\a"
#     echo -ne "\033]6;1;bg;blue;brightness;255\a"
#     ;;
# esac

# Option 3: Custom Powerlevel10k prompt cursor per machine
# Note: This must be set BEFORE p10k is initialized
# case "$(hostname -s)" in
#   dookbox)
#     # Change cursor symbol and color on Linux box
#     typeset -g POWERLEVEL9K_PROMPT_CHAR_{OK,ERROR}_VIINS_CONTENT_EXPANSION='⚡'
#     typeset -g POWERLEVEL9K_PROMPT_CHAR_OK_{VIINS,VICMD,VIVIS,VIOWR}_FOREGROUND=33  # Blue
#     ;;
#   macbook)
#     # Different cursor for macOS
#     typeset -g POWERLEVEL9K_PROMPT_CHAR_{OK,ERROR}_VIINS_CONTENT_EXPANSION='➜'
#     typeset -g POWERLEVEL9K_PROMPT_CHAR_OK_{VIINS,VICMD,VIVIS,VIOWR}_FOREGROUND=76  # Green
#     ;;
# esac

# Option 4: Custom Powerlevel10k colors per machine (other elements)
# case "$(hostname -s)" in
#   dookbox)
#     POWERLEVEL9K_DIR_BACKGROUND='blue'
#     POWERLEVEL9K_DIR_FOREGROUND='white'
#     ;;
# esac

# ------------------------------------------------------------------------------
# Python Version (if you have a specific version installed)
# ------------------------------------------------------------------------------
# export python=/opt/homebrew/bin/python3.10
# Or add a specific Python to PATH:
# export PATH="/opt/homebrew/opt/python@3.11/bin:$PATH"

# ------------------------------------------------------------------------------
# PostgreSQL (if installed via Homebrew)
# ------------------------------------------------------------------------------
# Add PostgreSQL to PATH
# export PATH="/opt/homebrew/opt/postgresql@17/bin:$PATH"
#
# Or create aliases for specific versions:
# alias psql=/opt/homebrew/opt/postgresql@17/bin/psql
# alias pg_dump=/opt/homebrew/opt/postgresql@17/bin/pg_dump
# alias pg_restore=/opt/homebrew/opt/postgresql@17/bin/pg_restore

# ------------------------------------------------------------------------------
# MongoDB (if installed via Homebrew)
# ------------------------------------------------------------------------------
# export PATH="/opt/homebrew/opt/mongodb-community@4.4/bin:$PATH"

# ------------------------------------------------------------------------------
# IDE Launchers (macOS-specific)
# ------------------------------------------------------------------------------
# Note: WebStorm alias is already in .zshrc
#
# Open VSCode from command line
# alias code='/Applications/Visual\ Studio\ Code.app/Contents/Resources/app/bin/code'
#
# Open IntelliJ IDEA from command line
# alias idea='open -na "IntelliJ IDEA.app" --args "$@"'
#
# Open PyCharm from command line
# alias pycharm='open -na "PyCharm.app" --args "$@"'

# ------------------------------------------------------------------------------
# Custom PATH additions
# ------------------------------------------------------------------------------
# Add your own custom bin directories
# export PATH="$HOME/custom-scripts:$PATH"
# export PATH="/usr/local/custom-tools/bin:$PATH"

# ------------------------------------------------------------------------------
# Environment Variables
# ------------------------------------------------------------------------------
# Custom JAVA_HOME for your machine
# export JAVA_HOME=$(/usr/libexec/java_home -v 17)
#
# Android SDK
# export ANDROID_HOME=$HOME/Library/Android/sdk
# export PATH=$PATH:$ANDROID_HOME/emulator
# export PATH=$PATH:$ANDROID_HOME/tools
# export PATH=$PATH:$ANDROID_HOME/tools/bin
# export PATH=$PATH:$ANDROID_HOME/platform-tools
#
# Go workspace
# export GOPATH=$HOME/go
# export PATH=$PATH:$GOPATH/bin

# ------------------------------------------------------------------------------
# Custom Aliases
# ------------------------------------------------------------------------------
# Machine-specific shortcuts
# alias myserver='ssh user@my-server.com'
# alias myproject='cd ~/Projects/my-favorite-project'
# alias docker-clean='docker system prune -af --volumes'

# ------------------------------------------------------------------------------
# Work-specific Settings
# ------------------------------------------------------------------------------
# Company VPN
# alias vpn-connect='sudo openconnect --protocol=gp vpn.company.com'
#
# Work-specific environment variables
# export WORK_API_URL="https://api.company.com"
# export WORK_ENV="development"
